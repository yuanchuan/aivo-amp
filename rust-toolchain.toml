# Exact pin so contributors + CI share one compiler/fmt/clippy. Must stay ≥ the
# `aivo` dep's declared MSRV — mirror aivo's own pin when bumping the dep.
[toolchain]
channel = "1.97.0"
components = ["rustfmt", "clippy"]
